# 金典软装销售系统 V3.0 — 环境变量模板
# 复制此文件为 .env 后填入真实值

# JWT 密钥（必填，生产环境必须从环境变量注入，禁止在代码库存储真实密钥）
JWT_SECRET=your-secret-key-here

# 允许的 CORS 源（逗号分隔，示例）
# ALLOWED_ORIGINS=http://localhost:3000,https://sales.jindian.com
ALLOWED_ORIGINS=

# 数据库（可选，默认使用 sales.db）
# DATABASE_URL=sqlite+aiosqlite:///./sales.db